# MineBuildCity

A Netlify-ready Minecraft city build planner for organizing districts, block palettes, and build milestones before starting a large city project.

## Run locally

```bash
npm start
```

## Build for Netlify

```bash
npm run build
```

Netlify is configured with `netlify.toml` to run the build command and publish the generated `dist` directory.
